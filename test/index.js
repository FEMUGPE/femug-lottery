const assert = require('assert');
const lottery = require('../lottery.js');

describe('lottery()', () => {

  // --------------------------

  it('Should return a random item from json', () => {
    //assert.equal();
  });

});
